package edu.fiuba.algo3;

import edu.fiuba.algo3.modelo.Personaje;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;

public class VistaPersonaje {
    /*
    private double personajeScale = 1;
    private VistaTablero stage;
    ImageView personajeImage;

    private Personaje personaje;

    private int lastXPosition;

    public PlayerView(VistaTablero stage, Personaje personaje) {
        this.stage = stage;
        this.personaje = personaje;

        this.lastXPosition = personaje.getPosicionActual().getValorHorizontal();

        personajeImage = new ImageView();
        personajeImage.setScaleX(personajeScale);
        personajeImage.setScaleY(personajeScale);
        personajeImage.setFitHeight(64);
        personajeImage.setFitWidth(64);

        personajeImage.setImage(new Image("https://i.imgur.com/DGV1IJc.png"));
        stage.addViewOnMap(personajeImage, personaje.getPosicionActual().getValorHorizontal(),
                personaje.getPosicionActual().getValorVertical());;
        draw();

    }

    */
}
